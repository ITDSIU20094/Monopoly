package UI;

import javax.swing.*;

public class AppSetting extends JFrame {
    public AppSetting(){
        Setting setting = new Setting();
        setting.setLocationRelativeTo(null);
    }

    
}
